# UI mockup drops (reference art)

Place **high-fidelity UI mockup PNGs** here when you copy them out of Cursor workspace storage or design exports.

**Rules**

- These files are **documentation / reference only** until promoted.
- **Do not** reference this folder from production React or from `lib/assets.ts` unless each file has been through **ASSET_PIPELINE** (rename, `public/assets/...`, registry).
- Prefer **lowercase hyphen** filenames matching `UI_MOCKUP_REFERENCE.md` in the parent folder.

See **`../UI_MOCKUP_REFERENCE.md`** for the screen index and canon notes (Bio / Mecha / **Pure** — not Spirit in shipped UI).
