export type ArenaMode = {
  title: string;
  subtitle: string;
  body: string;
};

export type QueueState = "idle" | "searching" | "matched";

export type ArenaAccent = {
  ring: string;
  glow: string;
  chip: string;
  bar: string;
};
